from setuptools import find_packages, setup
setup(
    name='Inputbetter',
    packages=find_packages(include=['Inputbetter']),
    version='0.5.1',
    description='A library to make entering data into lists easier!',
    author='Sankie',
    license='MIT'
)
